<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="ToolTipBackgroundBrush" Color="{DynamicResource ToolTipBackgroundColor}"/>
    <SolidColorBrush x:Key="ToolTipBorderBrush" Color="{DynamicResource ToolTipBorderColor}"/>

    <Style TargetType="ToolTip">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border BorderBrush="{DynamicResource ToolTipBorderBrush}" BorderThickness="1" Background="{DynamicResource ToolTipBackgroundBrush}">
                        <ContentPresenter ContentSource="Content" Margin="{TemplateBinding Padding}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>